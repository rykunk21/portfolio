[build]
target = "index.html"

[tools]
# Force trunk to use the system-installed wasm-bindgen instead of downloading its own
wasm_bindgen = "0.2.95"

[watch]
ignore = ["dist", "target"]

[serve]
address = "0.0.0.0"
port = 8080